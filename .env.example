# Optional — only needed for OAuth (private Google Doc import).
# Public/shared docs do NOT require these.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
